---
- hosts: localhost
  connection: local
  become: yes  # runs everything as root
  roles:
    - initial-setup
    - notifier
    - seasontracker
    - weather-station-server
    - docker
    - immich-scripts
