---
- hosts: localhost
  connection: local
  become: yes  # runs everything as root
  roles:
    - initial-setup
    - caddy
    - notifier
    - seasontracker
    - weather-station-server
    - email-archive
    - docker
    - grafana
    - papra
    - jellyfin
    - immich-scripts
