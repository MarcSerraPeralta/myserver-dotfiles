---
- hosts: localhost
  connection: local
  become: yes  # runs everything as root
  roles:
    - initial-setup
    - notifier
    - seasontracker
    - weather-station-server
    - email-archive
    - docker
    - grafana
    - jellyfin
    - immich-scripts
