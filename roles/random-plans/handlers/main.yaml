---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart random-plans
  ansible.builtin.systemd:
    name: random-plans
    state: restarted
  become: true
